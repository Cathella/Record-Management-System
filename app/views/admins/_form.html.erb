<%= form_with(model: admin, local: true) do |form| %>
  <% if admin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin.errors.count, "error") %> prohibited this admin from being saved:</h2>

      <ul>
      <% admin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field mb-3">
    <%= form.label :username, class: "text-muted" %>
    <%= form.text_field :username, class: "form-control py-4" %>
  </div>

  <div class="field mb-3">
    <%= form.label :password, "Password",  class: "text-muted" %>
    <%= form.password_field :password, class: "form-control py-4" %>
  </div>

  <div class="field mb-4">
    <%= form.label :password_confirmation, "Confirm Password",  class: "text-muted" %>
    <%= form.password_field :password_confirmation, class: "form-control py-4" %>
  </div>

  <div class="actions">
    <%= form.submit "Create Account", class: "btn btn-info rounded-btn w-100 font-weight-bold" %>
  </div>
<% end %>
